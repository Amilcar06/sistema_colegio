server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to backend (if not handling CORS directly from backend in prod)
    # location /api {
    #     proxy_pass http://backend:8080;
    # }
}
