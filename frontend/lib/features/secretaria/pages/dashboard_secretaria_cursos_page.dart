import 'package:flutter/material.dart';
import '../../../shared/widgets/main_scaffold.dart';

class DashboardSecretariaCursosPage extends StatelessWidget {
  const DashboardSecretariaCursosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const MainScaffold(
      child: Center(child: Text('Página: Cursos')),
    );
  }
}
