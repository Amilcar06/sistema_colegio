import 'package:flutter/material.dart';
import '../../../shared/widgets/main_scaffold.dart';

class DashboardDirectorInscripcionesPage extends StatelessWidget {
  const DashboardDirectorInscripcionesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const MainScaffold(
      child: Center(child: Text('Página: Inscripciones')),
    );
  }
}
